import React, { FunctionComponent } from 'react'

type Props = {}

const ChatHeader: FunctionComponent<Props> = (props: Props) => {
    return <div className='chat-header'>Conversation Name</div>
}

export default ChatHeader